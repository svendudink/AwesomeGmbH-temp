version: "3"

services:
  server:
    build:
      context: .
      dockerfile: server.Dockerfile
    ports:
      - "8080:8080"

  client:
    build:
      context: .
      dockerfile: client.Dockerfile
    ports:
      - "3000:80"
